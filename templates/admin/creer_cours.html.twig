{# templates/admin/creer_cours.html.twig #}
<h1>Créer un Cours</h1>
{{ form_start(form) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.professeur) }}
    <button type="submit">Créer</button>
{{ form_end(form) }}
